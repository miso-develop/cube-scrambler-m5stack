#pragma once

// Copy this file to include/wifi_credentials.h and edit the values.
// include/wifi_credentials.h is ignored by Git and must never be committed.
#define CUBE_WIFI_SSID "YOUR_SSID"
#define CUBE_WIFI_PASSWORD "YOUR_PASSWORD"
